<div class="featured-products" id="featured-products--{{ section.id }}">
{%- if section.settings.divider -%}<div class="section--divider">{%- endif -%}

<div class="page-width">
  {%- if section.settings.title != blank or section.settings.text != blank -%}
    <div class="section-header text-left">
      <h2>{{ section.settings.title }}</h2>
      {%- if section.settings.text != blank -%}
       <div class="rte">{{ section.settings.text }}</div>
      {%- endif -%}
      <div class="product-toggle">
        <div id="premium" class="selected">Premium Pure</div>
        <div class="toggle"></div>
        <div id="extra_strength">Extra Strength</div>
      </div>
    </div>
  {%- endif -%}

  <div class="grid{% unless section.blocks.size == 5 %} grid--uniform{% endunless %} grid--flush-bottom">
    {%- liquid
      assign grid_item_width = 'medium-up--one-third'
      case section.blocks.size
        when 1
          assign grid_item_width = 'medium-up--one-half'
        when 2
          assign grid_item_width = 'medium-up--one-half'
        when 4
          assign grid_item_width = 'medium-up--one-half'
      endcase
    -%}
    {%- for block in section.blocks -%}
      {%- if section.blocks.size == 5 and forloop.index < 3 -%}
        {%- assign column_width = 'medium-up--one-half' -%}
      {%- else -%}
        {%- assign column_width = grid_item_width -%}
      {%- endif -%}
      <div class="grid__item {{ column_width }} text-{{ section.settings.align_text }}" {{ block.shopify_attributes }} 
           data-aos="row-of-3" 
           data-type="{{ block.settings.type }}"
           {% if block.settings.type == "extra_strength" %}style="display: none;"{% endif %}>
      {% if block.settings.banner != blank %}
        <div class="featured-product--banner">{{ block.settings.banner }}</div>
      {% endif %}
      {%- if block.settings.enable_image -%}
          <div style="{% if section.settings.align_text == 'center' %}margin: 0 auto;{% endif %}">
            {%- if block.settings.button_link != blank -%}
              <a href="{{ block.settings.button_link }}">
            {%- endif -%}
            {%- if block.settings.image != blank -%}
              <div class="image-wrap text-spacing" style="height: 0; padding-bottom: {{ 100 | divided_by: block.settings.image.aspect_ratio }}%;">
                {%- assign img_url = block.settings.image | img_url: '1x1' | replace: '_1x1.', '_{width}x.' -%}
                <img class="lazyload"
                    data-src="{{ img_url }}"
                    data-widths="[180, 360, 540, 720, 900, 1080]"
                    data-aspectratio="{{ block.settings.image.aspect_ratio }}"
                    data-sizes="auto"
                    alt="{{ block.settings.image.alt }}">
              </div>
            {%- else -%}
              <div class="image-wrap text-spacing">{{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}</div>
            {%- endif -%}
            {%- if block.settings.button_link != blank -%}
              </a>
            {%- endif -%}
          </div>
        {%- endif -%}
        
        {%- if block.settings.title != blank -%}
          <div class="product-heading">{{ block.settings.title }}</div>
        {%- endif -%}
        
        {% if block.settings.price != blank %}
        <div class="grid-product__price">
          {% if block.settings.price != blank %}
           <div class="compare-price">{{ block.settings.compare_price }}</div>
          {% endif %}
          <div class="price">{{ block.settings.price }}</div>
        </div>
        {% endif %}
        {% if block.settings.text_1 != blank %}
        <ul class="benefits-list">
           {% for i in (1..3) %}
            {% assign text = "text_" | append: i %}
            {% assign text = block.settings[text] | default: false %}
            {% if text != blank %}
             <li>{{ text }}</li>
            {% endif %}
           {% endfor %}
         </ul>
        {% endif %}
        {%- if block.settings.button_label != blank -%}
          <a href="{{ block.settings.button_link }}" class="btn btn--primary">
            {{ block.settings.button_label }}
          </a>
        {%- endif -%}
      </div>
    {%- endfor -%}
  </div>
</div>

{%- if section.settings.divider -%}</div>{%- endif -%}
</div>
  
<style>
  #featured-products--{{ section.id }} {
    background: {{ section.settings.background }};
  }
  #featured-products--{{ section.id }}{
    color: {{ section.settings.text_color }};
  }
  #featured-products--{{ section.id }} h2 {
    color: {{ section.settings.heading_color }};
  }
</style>

<script>
  var toggle = document.querySelector(".product-toggle");
  var toggleBtn = document.querySelector(".toggle");
  var premium = document.getElementById("premium");
  var extra = document.getElementById("extra_strength");
  var premiumProducts = document.querySelectorAll("[data-type='premium']");
  var extraProducts = document.querySelectorAll("[data-type='extra_strength']");
  toggleBtn.addEventListener('click', function(event) {
    if (toggle.classList.contains('alternate')) {
      toggle.classList.remove("alternate");
      premium.classList.add("selected");
      extra.classList.remove("selected");
      for (var i = 0; i < premiumProducts.length; ++i) {
       premiumProducts[i].style.display = "block";
      }
      for (var i = 0; i < extraProducts.length; ++i) {
       extraProducts[i].style.display = "none";
      }
    } else {
      toggle.classList.add("alternate");
      extra.classList.add("selected");
      premium.classList.remove("selected");
      for (var i = 0; i < premiumProducts.length; ++i) {
       premiumProducts[i].style.display = "none";
      }
      for (var i = 0; i < extraProducts.length; ++i) {
       extraProducts[i].style.display = "block";
      }
    }
  });
</script>

{% schema %}
  {
    "name": "Featured products",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Heading"
      },
      {
        "type": "richtext",
        "id": "text",
        "label": "Text",
        "default": "<p>Use this section to explain a set of product features, to link to a series of pages, or to answer common questions about your products. Add images for emphasis.</p>"
      },
      {
        "type": "select",
        "id": "align_text",
        "label": "Alignment",
        "default": "center",
        "options": [
          {
            "value": "left",
            "label": "Left"
          },
          {
            "value": "center",
            "label": "Centered"
          },
          {
            "value": "right",
            "label": "Right"
          }
        ]
      },
      {
        "type": "checkbox",
        "id": "divider",
        "label": "Show section divider",
        "default": false
      },
      {
        "type": "color",
        "id": "background",
        "label": "Background",
        "default": "#fff"
      },
      {
        "type": "color",
        "id": "heading_color",
        "label": "Heading color",
        "default": "#002D72"
      },
      {
        "type": "color",
        "id": "text_color",
        "label": "Text color",
        "default": "#0E1111"
      }
    ],
    "blocks": [
      {
        "type": "product",
        "name": "Product",
        "settings": [
          {
            "type": "select",
            "id": "type",
            "label": "Type",
            "default": "premium",
            "info": "Used to determine what shows in toggled selection",
            "options": [
              {
                "value": "premium",
                "label": "Premium Pure"
              },
              {
                "value": "extra_strength",
                "label": "Extra strength"
              }
            ]
          },
          {
            "type": "text",
            "id": "banner",
            "label": "Banner"
          },
          {
            "type": "checkbox",
            "id": "enable_image",
            "label": "Show image",
            "default": true
          },
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          },
          {
            "type": "text",
            "id": "title",
            "label": "Heading",
            "default": "Example title"
          },
          {
            "type": "text",
            "id": "price",
            "label": "Price",
            "default": "$XX"
          },
          {
            "type": "text",
            "id": "compare_price",
            "label": "Compare at price"
          },
          {
            "type": "text",
            "id": "text_1",
            "label": "List item #1",
            "default": "Lorem ipsum dolor sit amet"
          },
          {
            "type": "text",
            "id": "text_2",
            "label": "List item #2",
            "default": "Lorem ipsum dolor sit amet"
          },
          {
            "type": "text",
            "id": "text_3",
            "label": "List item #3",
            "default": "Lorem ipsum dolor sit amet"
          },
          {
            "type": "text",
            "id": "button_label",
            "label": "Button label",
            "default": "Buy Now"
          },
          {
            "type": "url",
            "id": "button_link",
            "label": "Link"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Featured products",
        "blocks": [
          {
            "type": "product"
          },
          {
            "type": "product"
          },
          {
            "type": "product"
          }
        ]
      }
    ]
  }
{% endschema %}
